import { PaletteOptions } from '@mui/material';

const colors: PaletteOptions = {
  mode: 'light',
  primary: { main: '#2f2f2f' },
  background: {
    default: '#fff',
    paper: '#fff',
  },
  sidebar: { main: '#ddd' },
};

export default colors;
