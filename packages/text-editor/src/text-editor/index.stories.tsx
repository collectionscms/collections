import React from 'react';
import { TextEditor } from './index';

export default {
  title: 'Components/TextEditor',
  component: TextEditor,
};

const Template = () => <TextEditor language="ja" />;

export const Default = Template.bind({});
